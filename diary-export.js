function allExportRows(){let dates=[...new Set([...records.map(r=>r.date),...notes.map(n=>n.date),...diaries.map(d=>d.date)])].sort();return dates.map(date=>{let r=records.find(x=>x.date===date)||{},diary=diaries.find(x=>x.date===date)||{};return Object.fromEntries([['日期',date],['记录位置',r.location||''],['今日核心目标',(r.goals||'').split(/\r?\n/).filter(Boolean).map((x,i)=>`${i+1}. ${x}`).join('\n')],...Q.map(q=>[q[1],(r[q[0]]||'').split(/\r?\n/).filter(Boolean).map((x,i)=>`${i+1}. ${x}`).join('\n')]),['随手记',notesForDate(date)],['随手记记录时间',noteTimesForDate(date)],['日记',diary.text||''],['日记地点',diary.place||'']])})}
function exportExcel(){if(!records.length&&!notes.length&&!diaries.length)return alert('还没有可导出的记录。');if(!window.XLSX)return alert('Excel 组件未加载，请联网后重试。');let ws=XLSX.utils.json_to_sheet(allExportRows());ws['!cols']=[{wch:13},{wch:20},{wch:35},...Q.map(()=>({wch:35})),{wch:42},{wch:20},{wch:50},{wch:28}];let wb=XLSX.utils.book_new();XLSX.utils.book_append_sheet(wb,ws,'每日复盘');let d=new Date(),p=n=>String(n).padStart(2,'0'),stamp=`${d.getFullYear()}${p(d.getMonth()+1)}${p(d.getDate())}_${p(d.getHours())}${p(d.getMinutes())}${p(d.getSeconds())}`;XLSX.writeFile(wb,`博士日课复盘记录_${stamp}.xlsx`)}
function excelBlob(){if(!window.XLSX)throw new Error('Excel 组件未加载，请联网后重试。');let ws=XLSX.utils.json_to_sheet(allExportRows());ws['!cols']=[{wch:13},{wch:20},{wch:35},...Q.map(()=>({wch:35})),{wch:42},{wch:20},{wch:50},{wch:28}];let wb=XLSX.utils.book_new();XLSX.utils.book_append_sheet(wb,ws,'每日复盘');return new Blob([XLSX.write(wb,{bookType:'xlsx',type:'array'})],{type:'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet'})}
$('#excel').onclick=exportExcel;
